// PlayerType.cs (или добавьте в GameManager.cs, если хотите все в одном файле)
public enum PlayerType {
    None, // Используется для неактивного состояния или когда игрок не назначен
    Player1,
    Player2
}